@model LivrariaSouza.Models.Models.Livro

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Livro</title>
</head>
<body>
    <h2>Criar Livro</h2>
    <form asp-action="CriarLivro" method="post">
        <div>
            <label>Título:</label>
            <input type="text" asp-for="Titulo" required />
        </div>
        <div>
            <label>Autor:</label>
            <input type="text" asp-for="Autor" required />
        </div>
        <div>
            <label>Número de Páginas:</label>
            <input type="number" asp-for="NumeroPag" required />
        </div>
        <div>
            <label>Valor:</label>
            <input type="text" asp-for="Valor" required />
        </div>
        <div>
            <label>Ano de Lançamento:</label>
            <input type="date" asp-for="AnoLancamento" required />
        </div>
        <div>
            <label>Descrição:</label>
            <textarea asp-for="Descricao" required></textarea>
        </div>
        <div>
            <label>Imagem:</label>
            <input type="text" asp-for="Imagem" required />
        </div>
        <div>
            <label>Disponibilidade:</label>
            <input type="checkbox" asp-for="Disponibilidade" onchange="toggleEstoque(this)" />
        </div>
        <div id="estoqueDiv">
            <label>Quantidade em Estoque:</label>
            <input type="number" asp-for="QntdEstoque" />
        </div>
        <button type="submit">Criar Livro</button>
    </form>

    <script>
        function toggleEstoque(checkbox) {
            const estoqueDiv = document.getElementById('estoqueDiv');
            if (!checkbox.checked) {
                estoqueDiv.querySelector('input').value = ''; // Limpa o campo de quantidade
            }
            estoqueDiv.style.display = checkbox.checked ? 'block' : 'none'; // Mostra/oculta o campo de estoque
        }

        // Inicializa a exibição correta ao carregar a página
        document.addEventListener('DOMContentLoaded', function () {
            toggleEstoque(document.querySelector('input[type="checkbox"]'));
        });
    </script>
</body>
</html>
